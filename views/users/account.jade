extends ../layout

block content

    br

    h1 Your role : 
        if user.role
            | User 
        else
            | Super Admin 
        a.btn.btn-info(href="edit_role/#{user._id}", role="button") Change your role

    br

    form(method="POST")
        .form-group
            input.form-control.form-control-lg#nameInput(type="text", name="username", value="#{user.username}")
        .form-group
            input.form-control.form-control-lg#emailInput(name="email", placeholder='email', value="#{user.email}")
        input.btn.btn-lg.btn-success(type="submit", value="Update your account") 


    br
    a.btn.btn-danger(href="delete/#{user._id}", role="button") Delete this account